runtime: nodejs
env: flex

manual_scaling:
  instances: 1

resources:
  cpu: 4
  memory_gb: 16
  disk_size_gb: 10

runtime_config:
  operating_system: "ubuntu22"
  runtime_version: "20"
env_variables:
  OPENAI_API_KEY: ""
